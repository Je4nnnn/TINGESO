server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_m5_kardex
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
